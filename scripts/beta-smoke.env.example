# Prefer: npm run beta:smoke:setup   (writes ../.env.smoke from prompts)
# Or copy this file to repo root as `.env.smoke` (gitignored via `.env.*`).

SUPABASE_PROJECT_REF=wslymzcbbevnoybbsbgq
SUPABASE_URL=https://wslymzcbbevnoybbsbgq.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# For ADMIN_SMOKE_EMAIL + password sign-in:
SUPABASE_ANON_KEY=

# Admin JWT path A: paste short-lived access_token from browser.
ADMIN_SMOKE_JWT=
# Admin path B:
# ADMIN_SMOKE_EMAIL=
# ADMIN_SMOKE_PASSWORD=

# Stripe (optional — enables balance + Dashboard webhook URL checks)
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_BIN_MONTHLY_PRICE_ID=price_...
# If you only use `stripe listen` (no Dashboard endpoint), set:
# STRIPE_FORWARDING_ONLY=1

# Optional CLI presence check: npm run beta:smoke:stripe
